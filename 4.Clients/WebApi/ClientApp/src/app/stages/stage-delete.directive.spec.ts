import { StageDeleteDirective } from './stage-delete.directive';

describe('StageDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new StageDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
