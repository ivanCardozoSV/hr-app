<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="HrApp.Views.CandidateView">
    <ContentPage.Content>

        <ListView x:Name="CandidateViw" Margin="20">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Name}" TextColor="Black"></Label>
                            <Label Text="{Binding LastName}" TextColor="Black"></Label>
                            <Label Text="{Binding DNI}" TextColor="Black"></Label>
                            <Label Text="{Binding PhoneNumber}" TextColor="Black"></Label>
                            <Label Text="{Binding EmailAddress}" TextColor="Black"></Label>
                            <Label Text="{Binding LinkedInProfile}" TextColor="Black"></Label>
                            <Label Text="{Binding AdditionalInformation}" TextColor="Black"></Label>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
    </ContentPage.Content>
</ContentPage>